# Adopt the existing protected pins; do not create a second tag ruleset.
import {
  to = github_repository_ruleset.reusable_workflow_pins["venus-os-ci-toolkit"]
  id = "venus-os-ci-toolkit:23833305"
}
